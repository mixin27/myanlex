# Local UI-only receiver. No email, chat or paging notifications are sent.
route:
  receiver: local-review-only
  group_by: [alertname, job]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: local-review-only
