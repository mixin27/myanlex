name: myanlex_dart
description:
  Typed HTTP client for MyanLex language processing, with cancellation and
  explicit quota-aware error handling.
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.13.1

dependencies:
  http: ^1.6.0
  http_parser: ^4.1.2

dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0
